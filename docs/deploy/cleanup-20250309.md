# 部署文件清理记录

## 📅 日期
2025-03-09

## 🎯 目标
根据本地部署成功经验，清理不需要的部署相关文件，优化生产环境配置。

## 🗑️ 已删除的文件

### apps/api/ 目录

1. **entrypoint.sh** (旧的入口脚本)
   - 原因：使用 `npx prisma migrate deploy`，存在路径和配置问题
   - 替代：使用 `entrypoint-simple.sh`（已验证可用）

2. **docker-entrypoint.sh** (开发环境脚本)
   - 原因：使用 `tsx watch`，仅用于开发环境
   - 替代：生产环境不需要此文件

### 项目根目录

3. **deploy.sh** (旧的部署脚本)
   - 原因：功能与新的 `scripts/deploy-production.sh` 重复
   - 替代：使用 `scripts/deploy-production.sh`（功能更完善）

## ✅ 保留的文件

### apps/api/ 目录

- **entrypoint-simple.sh** ✅
  - 状态：正在使用
  - 用途：生产环境容器入口脚本
  - 验证：本地环境已验证可用
  - 关键特性：
    - 在 `infra/database` 目录运行迁移
    - 正确处理 `DATABASE_URL`
    - 兼容 Prisma 7 配置

### scripts/ 目录

- **clear-database.sh** ✅
  - 状态：新创建
  - 用途：清理数据库（开发/测试环境）

- **deploy-production.sh** ✅
  - 状态：新创建
  - 用途：生产环境快速部署脚本
  - 功能：
    - 环境检查
    - 镜像构建
    - 服务启动
    - 健康检查
    - 日志查看

- **docker-dev.sh** ✅
  - 状态：保留
  - 用途：开发环境快速启动脚本

- **test-docker-build.sh** ✅
  - 状态：保留
  - 用途：测试 Docker 镜像构建

### docker-compose 文件

- **docker-compose.prod.yml** ✅ (已优化)
- **docker-compose.local.yml** ✅
- **docker-compose.dev.yml** ✅
- **docker-compose.db.yml** ✅

## 📝 新增的文件

### 配置文件

- **.env.prod.example** - 生产环境变量模板

### 文档

- **docs/deploy/production.md** - 生产环境部署详细指南
- **docs/deploy/README.md** - 部署文档索引
- **docs/deploy/summary.md** - 优化总结
- **docs/deploy/cleanup-20250309.md** - 本文件

## 🔧 优化的配置

### docker-compose.prod.yml

**新增配置**：
1. 日志管理（最大 10MB，保留 3 个文件）
2. 资源限制（CPU 和内存）
3. 健康检查优化（启动时间 60s）

**配置详情**：
```yaml
logging:
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"

deploy:
  resources:
    limits:
      cpus: '1.0'
      memory: 1G
```

## ✨ 验证结果

### 本地环境验证

- ✅ 数据库迁移成功执行
- ✅ 所有 16 个表正确创建
- ✅ 应用成功启动
- ✅ API 服务健康检查通过
- ✅ Admin 服务健康检查通过

### 关键修复

1. **Prisma 迁移问题**
   - 问题：CLI 路径和配置问题
   - 解决：在 `infra/database` 目录运行迁移
   - 验证：成功执行 14 个迁移

2. **Prisma 7 兼容性**
   - 问题：不再支持在 schema.prisma 中定义 url
   - 解决：使用 prisma.config.ts 配置
   - 验证：迁移正常执行

## 📊 文件统计

### 删除的文件
- 3 个脚本文件

### 新增的文件
- 1 个环境变量模板
- 4 个文档文件
- 1 个部署脚本

### 优化的文件
- 1 个 docker-compose 配置

## 🎉 总结

通过本次清理和优化：

1. **简化了部署流程**
   - 统一使用 `scripts/deploy-production.sh`
   - 清理了冗余的入口脚本
   - 提供了完整的部署文档

2. **增强了生产环境配置**
   - 添加资源限制
   - 配置日志管理
   - 优化健康检查

3. **提高了可维护性**
   - 详细的部署文档
   - 清晰的文件结构
   - 完善的使用说明

## 📚 相关文档

- [优化总结](./summary.md)
- [生产环境部署指南](./production.md)
- [部署文档首页](./README.md)
